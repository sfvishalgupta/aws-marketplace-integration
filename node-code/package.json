{
  "name": "aws-marketplace",
  "version": "1.0.0",
  "description": "",
  "main": "start.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "export-profile": "export AWS_PROFILE=MP_ADMIN",
    "deploy": "npm run export-profile && npm run s3-sync && sls deploy",
    "s3-sync": "cd web && npm run export-profile && aws s3 cp --recursive ./ s3://test-bucket-sf-marketplace --acl public-read"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-marketplace-catalog": "^3.629.0",
    "aws-sdk": "^2.1678.0",
    "nodemailer": "^6.9.14",
    "winston": "^3.14.2"
  },
  "devDependencies": {
    "serverless-ssm-fetch": "^2.0.0"
  }
}
